student(sid(pk),firstname,lastname, address,email,phone,dob,gender)
course(cid(pk),coursename,description,degree,min,max)
studentcourse(sid(fk),cid(fk),grade)
instructor(inst_id(pk),firstname,lastname,address,email,phone,gender,dob,manager_id(recursive))
instructorcourse(inst_id(fk),cid(fk))
exam(exam_id(pk),cid(fk),type,total_degree,starttime,endtime,duration)
studentexam(sid(fk),exam_id(fk))
question(question_id(pk),exam_id(fk),question,type,correctanswer,questiongrade)
intake(intake_id(pk),number)
branch(branch_id(pk),name)
track(track_id(pk),name)
department(dept_id(pk),name)
dept_branch_intake_track(dept_id(fk),branch_id(fk),intake_id(fk),track_id(fk))
department_course(dept_id(fk),cid(fk))
student_answer(sid(fk),exam_id(fk),Q_id(fk),answer)

